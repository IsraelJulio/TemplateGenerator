
  <ItemGroup Label="Persistência — SQLite">
    <!-- O provedor traz o EF Core junto, na versão que ele mesmo exige. Declarar o EF Core de novo
         aqui abriria a porta para duas restrições de versão discordarem. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" Version="10.0.12" />
  </ItemGroup>
